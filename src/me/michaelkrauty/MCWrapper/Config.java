package me.michaelkrauty.MCWrapper;

/* import java.io.File;
 * import java.io.IOException;
 * import java.io.InputStream;
 * 
 * import org.yaml.snakeyaml.Yaml;
 */
public class Config {

	public Config() {
		/*
		 * File configFile = new File("/var/mcwrapper/configuration.yml"); if
		 * (configFile.exists()) { Yaml yaml = new Yaml(); InputStream test =
		 * new InputStream(); yaml.load(test); } else { try {
		 * configFile.createNewFile(); } catch (IOException e) {
		 * e.printStackTrace(); } }
		 */
	}
}
